import React, { Component } from 'react';

class Messenger extends Component {
    render(){
        return(
            <h1>This is the messenger Element</h1>
        );
    }
}

export default Messenger;